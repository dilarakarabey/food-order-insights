interface:
  display_name: "Food Order Insights"
  short_description: "Analyze supported Türkiye orders carefully"
  default_prompt: "Use $food-order-insights to answer my question from supported Türkiye food-order emails. Only derive metrics with enough data and explain important omissions."
dependencies:
  tools:
    - type: "mcp"
      value: "gmail"
      description: "Gmail search and read tools for receipt emails"
policy:
  allow_implicit_invocation: true
